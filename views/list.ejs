<title><%= title %></title>
<%= user.name %>
<%= user.contact %>
<form action="/products/createproduct" method="post" enctype="multipart/form-data">
    <input type="text" name="pname" placeholder="Product Name" value="naem" required>
    <input type="text" name="pprice" placeholder=" Price " value="66" required>
    <input type="file" name="pimg" placeholder="Umpload image" required>
    <input type="text" name="user" value="<%= user._id %>" hidden required>
    <input type="submit" value="Subimt">
</form>
<p>want to delet product ? </p>
<%for (i of all_users.products) { %>
    <div>
        <form action="/products/destroy/<%= i.id %>"
            method="get">
            <button type="submit"
                class="delete-coment-button">X</button>
        </form>
        
        <%= i.pname %> 
        <%= i.pprice %>
    </div>
    <% } %>
<a href="/">CLick</a>